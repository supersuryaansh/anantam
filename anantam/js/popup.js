// ANANT HACK POP
function anantPOP() {
  const modelWindow = document.querySelector("#modelWindowAnant");
  const anantPopBtn = document.querySelector("#anant--pop-btn");
  const popCloseBtn = document.querySelector("#anant--close-btn");

  anantPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
anantPOP();

// ROBO POP
function roboPOP() {
  const modelWindow = document.querySelector("#modelWindowRobo");
  const roboPopBtn = document.querySelector("#robo--pop-btn");
  const popCloseBtn = document.querySelector("#robo--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
roboPOP();

// CRUNCH POP
function crunchPop() {
  const modelWindow = document.querySelector("#modelWindowCrunch");
  const roboPopBtn = document.querySelector("#crunch--pop-btn");
  const popCloseBtn = document.querySelector("#crunch--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
crunchPop();

// ESPORTS POP
function esportsPop() {
  const modelWindow = document.querySelector("#modelWindowEsports");
  const roboPopBtn = document.querySelector("#esports--pop-btn");
  const popCloseBtn = document.querySelector("#esports--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
esportsPop();

//BID BOUNDARIES
function bidPop() {
  const modelWindow = document.querySelector("#modelWindowBid");
  const roboPopBtn = document.querySelector("#bid--pop-btn");
  const popCloseBtn = document.querySelector("#bid--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
bidPop();
// DALAL STREET
function dalalPop() {
  const modelWindow = document.querySelector("#modelWindowDalal");
  const roboPopBtn = document.querySelector("#dalal--pop-btn");
  const popCloseBtn = document.querySelector("#dalal--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
dalalPop();
// BINARY BRAINS
function binaryPop() {
  const modelWindow = document.querySelector("#modelWindowBinary");
  const roboPopBtn = document.querySelector("#binary--pop-btn");
  const popCloseBtn = document.querySelector("#binary--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
binaryPop();
// MANGA MANIA
function mangaPop() {
  const modelWindow = document.querySelector("#modelWindowManga");
  const roboPopBtn = document.querySelector("#manga--pop-btn");
  const popCloseBtn = document.querySelector("#manga--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
mangaPop();
// FRAMEFLIX
function framePop() {
  const modelWindow = document.querySelector("#modelWindowFrame");
  const roboPopBtn = document.querySelector("#frame--pop-btn");
  const popCloseBtn = document.querySelector("#frame--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
framePop();
// PIXEL PERFECT
function pixelPop() {
  const modelWindow = document.querySelector("#modelWindowPixel");
  const roboPopBtn = document.querySelector("#pixel--pop-btn");
  const popCloseBtn = document.querySelector("#pixel--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
pixelPop();
// BLIND DATE
function blindPop() {
  const modelWindow = document.querySelector("#modelWindowBlind");
  const roboPopBtn = document.querySelector("#blind--pop-btn");
  const popCloseBtn = document.querySelector("#blind--close-btn");

  roboPopBtn.addEventListener("click", function (e) {
    e.preventDefault();
    console.log("clickedddd");
    modelWindow.style.display = `flex`;
    modelWindow.style.opacity = 1;
  });
  popCloseBtn.addEventListener("click", function (e) {
    e.preventDefault();
    modelWindow.style.display = `none`;
    modelWindow.style.opacity = 0;
  });
}
blindPop();
